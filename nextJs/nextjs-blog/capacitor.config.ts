import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chriscatto.app',
  appName: 'CattoFunTrialApp',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
